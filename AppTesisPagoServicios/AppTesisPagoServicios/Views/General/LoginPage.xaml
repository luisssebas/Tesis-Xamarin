<?xml version="1.0" encoding="utf-8" ?>
<ContentPage 
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:d="http://xamarin.com/schemas/2014/forms/design"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    BackgroundColor="White"
    x:Class="AppTesisPagoServicios.Views.LoginPage">
    <ContentPage.Content>
        <StackLayout Margin="20" VerticalOptions="CenterAndExpand">
            <Image Source="banner" Margin="0,0,0,30"/>
            <Label Text="Usuario"/>
            <Entry Placeholder="Usuario" Text="{Binding Login.Usuario}"/>
            <Label Text="Contraseña"/>
            <Entry Placeholder="Contraseña" IsPassword="True" Text="{Binding Login.Contrasenia}"/>
            <StackLayout Margin="20">
                <Button 
                Text="Ingresar" 
                Command="{Binding IngresarCmd}"/>
                <Button 
                Text="Registrar" 
                Command="{Binding RegistrarCmd}"/>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>